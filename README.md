# dwarfgpt

Attempt at a bilingual GPT-2-scale model that speaks English and Neo-Khuzdul, by
mixing a synthetic Neo-Khuzdul corpus into a [nanochat](https://github.com/karpathy/nanochat)
pretraining run. Full plan in [`khuzdul_nanochat_plan.md`](khuzdul_nanochat_plan.md).

## Repository layout

```
.
├── khuzdul_nanochat_plan.md      master project plan
├── sentence_maker_port_plan.md   Phase 0 sub-plan for porting the Sentence Maker
├── sentence_maker_original_v1.xlsm   pristine copy of The Dwarrow Scholar's
│                                      DS-NKh-Sentence Maker.xlsm (do not edit)
├── khuzdul_translator/            ← Phase 0 deliverable: Python port of the
│                                    Sentence Maker (see its README)
├── data/                         JSON extracted from the .xlsm (215K dict +
│                                  rule tables); regenerated by
│                                  scripts/extract_tables.py
├── notes/                        archaeology / inventory notes
│   ├── sentence_maker_inventory.md
│   ├── sentence_maker_deep.md
│   └── vba_dump.txt
├── scripts/                      one-off Python utilities
│   ├── inspect_workbook.py
│   ├── inspect_deep.py
│   ├── extract_tables.py
│   ├── build_headwords.py       Phase 1: data/dictionary.json → headwords.json
│   └── score_gold_set.py
├── pyproject.toml                uv-managed project (Python ≥3.12)
└── Makefile                      make install / test / score / extract
```

## Phase 0 status

- [x] Inventory the .xlsm — see `notes/sentence_maker_inventory.md`
- [x] Extract reference tables to JSON — `data/*.json`, generated by `make extract`
- [x] Scaffold `khuzdul_translator` Python package — pip-installable via uv
- [x] Unit + smoke tests — `make test` (29 passing as of this commit)
- [x] 20-sentence gold set verified against the live Excel — hand-confirmed all
      20 entries match (user verified on a separate machine; `excel_*` fields in
      `khuzdul_translator/tests/gold_set.json` are unpopulated but the
      python-side values were the source of truth)
- [x] Passed Phase 0 decision gate (≥80% Khuzdul accuracy on the gold set)

Phase 1 (corpus assembly) is now in progress — see `khuzdul_nanochat_plan.md`.

### Phase 1 progress

- [x] Downloaded 77 Neo-Khuzdul Support Document PDFs + 1 combined PDF from
      `https://www.dwarrowscholar.com/library.html` into `corpus/pdfs/`
      (gitignored). Fetcher: `scripts/fetch_pdfs.py`. Manifest:
      `corpus/pdfs/_index.json` (sha256 + bytes per file).
- [x] Derived headword dictionary from the Excel extract: `data/headwords.json`
      (~6.5K headwords, 1 per (radical, broad-POS) plus 1 per radical-less
      function word). Schema: id / root / part_of_speech / entry / gloss /
      inflection_class / plural_form / irregular / n_variants / canonical_tag.
      Builder: `scripts/build_headwords.py`. Inflected variants in the
      gitignored `data/headword_variants.json` (62 MB).
- [ ] Locate the 2 standalone PDF dictionaries (not on `library.html`)
- [/] Extract grammar PDFs into machine-readable schemas
  - [x] `N-04 Noun Types.pdf` → `data/grammar/noun_types.json` (27 types,
        99.6% cross-validate against Excel inflection_class values)
  - [ ] `N-01 Personal Pronouns.pdf` (≥85 forms across 9 cases)
  - [ ] `N-02 The Verb.pdf` / `N-03 Verb Forms.pdf`
  - [ ] `N-05 Construct State.pdf`
  - [ ] `N-15 Geminate Consonants.pdf` (already partly encoded in Excel)
  - [ ] `N-16 Stress.pdf`
  - [ ] `N-38 Imperative and Jussive.pdf`
  - [ ] …plus ~70 more topical PDFs
- [ ] Assemble ≥3K-token seed corpus
- [ ] User-driven: reach out to The Dwarrow Scholar

## Quickstart

If you have `make`:

```
make install                # uv sync, installs deps into .venv
make test                   # run the Python module tests
make extract                # regenerate data/*.json from the .xlsm (slow, ~3 min)
```

Or directly via `uv` (works on plain Windows without GNU make):

```
uv sync
uv run pytest khuzdul_translator/tests/ -v
uv run python scripts/extract_tables.py
uv run python -m khuzdul_translator --phrase "dwarves"
```

## Credits

The dictionary and phonological / Cirth rule data come from
[The Dwarrow Scholar](https://thedwarrowscholar.com), creator of the
*DS-NKh-Sentence Maker* workbook. All Neo-Khuzdul scholarship is his.
Coordination with him for any public release of derived data is a master-plan
item (see Phase 1 in `khuzdul_nanochat_plan.md`).
